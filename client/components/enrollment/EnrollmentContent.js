import { Layout } from "antd";
import React from "react";

const { Content } = Layout;

function EnrollmentContent({}) {
	return (
		<Layout>
			<Content>Content kos</Content>
		</Layout>
	);
}

export default EnrollmentContent;
